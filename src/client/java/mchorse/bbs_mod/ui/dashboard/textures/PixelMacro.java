package mchorse.bbs_mod.ui.dashboard.textures;

/**
 * The one-shot operations of the texture editor over a region of the active layer — the
 * selection, or the frame on show without one; whole images of the strip from the frames.
 */
public enum PixelMacro
{
    CLEAR,
    FLIP_HORIZONTAL,
    FLIP_VERTICAL
}
