# BBS AI Studio · 开发思路总纲（DEVELOPMENT）

> 本文档是整个项目的开发思路梳理：产品定位 → 架构分层 → 功能地图 → 界面示意 →
> 数据流 → 路线图。新协作者读完应能理解「为什么这样设计、每块在哪、下一步做什么」。
> 架构细节见 ARCHITECTURE.md，日常维护见 MAINTENANCE.md。
>
> 决策记录（2026-09-25 用户确认）：
> ① 「任意界面语义调整」采用**面板侧边对话栏**形态；
> ② 视频识别定位为**核心功能**继续投入；
> ③ 下一轮优先级：界面打磨 / 语义调整执行层 / 锚定深化 / 版本跟进与文档（四项全做）。

---

## 一、产品定位

**BBS AI Studio = 给 BBS FS 拍摄框架加一层 AI 驱动**。两条主线：

1. **动画生成（AI 侧）**——用大模型把想法变成镜头与动作：
   对话 → 分镜/动作数据 → 预览 → 烘焙进影片。
2. **拍摄辅助（原生侧）**——把 AI 嵌进 BBS 原生工作流：
   视频动捕、mod 环境理解、锚点跟随、全界面上下文感知。

设计原则（历史教训的固化）：

- **两线分离**：生成类功能集中在 AI 编辑器；辅助类功能留在各原生面板——
  不混在一起（历史上混在工具面板导致功能割裂）。
- **任何 AI 产出必走正规通道**：分镜→Films、动作→PreviewSystem（预览确认后烘焙）、
  不可绕过；因此**全部可撤销**（AIUndoManager）。
- **上下文只有一个真相来源**：AIWorkspaceContext（你在哪个面板、编辑什么影片/模型），
  所有 AI 功能从这里读，不允许各功能自建碎片状态。
- **兼容性第一**：同一份 bbs_ai 源码面对 2.5/2.6/2.7 三代上游 + 四个 MC 版本；
  2.7-only API 隔离 + BBSFSCompat 运行时守卫。

---

## 二、架构分层

```
┌─────────────────────────────────────────────────────────────────┐
│  界面层（client）                                                │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │ AI 编辑器     │  │ AI 工具面板   │  │ 各原生面板 AI 按钮    │  │
│  │ 生成类主界面  │  │ 辅助类聚合    │  │ 影片/模型/变形(+扩展) │  │
│  └──────┬───────┘  └──────┬───────┘  └──────────┬───────────┘  │
│         │                 │                      │              │
│  ┌──────▼─────────────────▼──────────────────────▼───────────┐  │
│  │ 面板侧边对话栏（规划中·下轮）  ← 语义调整执行层              │  │
│  └────────────────────────┬──────────────────────────────────┘  │
└───────────────────────────┼─────────────────────────────────────┘
┌───────────────────────────▼─────────────────────────────────────┐
│  上下文层：AIWorkspaceContext（面板/影片/模型/变形/选中）          │
│            AIContextService（mod 环境摘要）  AIChatSession（历史） │
├─────────────────────────────────────────────────────────────────┤
│  服务层：AIServiceManager（厂商/Key 加密）  AIUndoManager（撤销）  │
│          PreviewSystem（预览/烘焙）  ImportManager  ModCompatScan │
│          ThemeManager（三主题全局色板）  BBSFSCompat（版本兼容）   │
├─────────────────────────────────────────────────────────────────┤
│  底座：AICore（BBSMod 主初始化启动）→ bbs-fs 2.5/2.6/2.7 兼容     │
└─────────────────────────────────────────────────────────────────┘
```

---

## 三、功能地图（状态：✅已实现 🔶部分 📋规划）

### 生成类（AI 编辑器，`Ctrl+Shift+S`）

| 功能 | 状态 | 说明 |
|---|---|---|
| Codex 式对话 | ✅ | 角色标头卡片、代码块暗盒、思维链/工具调用日志 |
| 分镜生成 | ✅ | storyboard 代码块 → 生成为影片/合并进当前影片（可撤销） |
| 动作生成 | ✅ | motion 代码块 → 预览系统（确认后烘焙） |
| 影片时间轴 | ✅ | 相机剪辑条块 + 角色轨道，点击选中 |
| 画面窗口 | ✅ | 世界实况 + 当前影片信息叠加 |
| 撤销/重做 | ✅ | Ctrl+Z/Y / 顶栏按钮 / 命令 |
| AI 设置 | ✅ | 服务商/Key/生成参数/模型映射/插件调用 |

### 辅助类（各原生面板 + 工具面板）

| 功能 | 状态 | 说明 |
|---|---|---|
| AI 服务配置 | ✅ | 工具面板 + AI 设置界面 |
| 视频动捕 | 🔶 | 真人视频→角色关键帧；需装 ONNX/FFmpeg；**定位核心功能，待打磨** |
| 导入管理 | ✅ | ai_cache/imports 扫描监听、预览烘焙 |
| 人物模型 | ✅ | 导出 .bbsm/浏览器/骨骼映射（模型编辑页） |
| 锚点跟随 | 🔶 | 约束面板 ✅ / 角色属性页(2.7) ✅ / 渲染链 🔶近似 / **绑定真实骨骼 📋** |
| Mod 兼容 | ✅ | 识别/知识库/适配器/报告/AI 上下文注入 |
| 面板 AI 按钮 | ✅ | 影片/模型编辑/变形（动作菜单） |
| **侧边对话栏** | 📋 | 下轮：任意面板侧边迷你对话，上下文限定当前面板 |

### 底座

| 功能 | 状态 |
|---|---|
| 版本兼容 2.5/2.6/2.7 + 四 MC 版本 | ✅ |
| 独立上下文系统 AIWorkspaceContext | ✅（对话/菜单已接入；侧边栏待接） |
| 全局主题三套（即时生效） | ✅ |
| 调试桥 + Python 工具链 | ✅ |

---

## 四、界面示意

### 4.1 AI 编辑器（生成类主界面）

```
┌──────────────────────────────────────────────┬──────────────────────────┐
│ ⚙撤销↶ ↷重做            状态：⟲ 最近AI操作…  │  [AI 编辑器顶栏按钮区]    │
├──────────────────────────────────────────────┼──────────────────────────┤
│ ┌ 取景框四角 ─────────────────────────────┐  │ ┌──────────────────────┐ │
│ │  影片画面窗口                            │  │ │ [AI] 卡片（色条+文字）│ │
│ │  （世界实况/演员/锚定预览）              │  │ │   正文自动换行       │ │
│ │                                          │  │ ├──────────────────────┤ │
│ │  叠加：当前影片信息                      │  │ │ ```storyboard```     │ │
│ │  暂存帧数/选中剪辑                      │  │ │ （代码块暗盒→按钮）  │ │
│ └──────────────────────────────────────────┘  │ ├──────────────────────┤ │
│                                               │ │ ▶ 生成为影片  (可撤销)│ │
│ 时间轴：相机剪辑▮▮▮▬ 角色轨道▬▬  [点击选中]  │ │ ...                  │ │
│                                               │ ├──────────────────────┤ │
│                                               │ │ 思维链/工具调用日志   │ │
│                                               │ │ · 调用AI服务 ✓ 512字 │ │
│                                               │ ├──────────────────────┤ │
│                                               │ │[环境分析][分镜草稿]  │ │
│                                               │ │[运镜建议][清空对话]  │ │
│                                               │ │输入框──────── [发送] │ │
└──────────────────────────────────────────────┴─┴──────────────────────┴─┘
   Ctrl+Shift+S 打开 · Ctrl+Enter 发送 · Ctrl+Z/Y 撤销重做
```

### 4.2 AI 工具面板（辅助聚合，折叠卡片式）

```
┌─────────────────────────────────────────┐
│ ▼ AI 设置      厂商/Key/URL/模型/测试   │
├─────────────────────────────────────────┤
│ ▶ 分镜生成     → 跳转 AI 编辑器        │
├─────────────────────────────────────────┤
│ ▶ 视频识别     视频路径/镜像/开始识别   │
├─────────────────────────────────────────┤
│ ▶ 导入管理     列表/预览/烘焙          │
├─────────────────────────────────────────┤
│ ▶ 模型         → 跳转人物模型编辑页    │
├─────────────────────────────────────────┤
│ ▼ IK 调整      约束面板/解算/烘焙      │
├─────────────────────────────────────────┤
│ ▶ Mod 兼容环境 列表/重扫/导出报告      │
├─────────────────────────────────────────┤
│ ▶ 界面         主题/语言/操作模式/热键 │
└─────────────────────────────────────────┘
   （滚动查看 · 底部热键状态栏）
```

### 4.3 AI 设置（弹窗，从编辑器顶栏齿轮）

```
┌─ AI 设置 ────────────────────── ✕ ─┐
│ ▼ AI 服务    厂商/Key/URL/模型/测试│
│ ▼ 生成参数   温度/Token            │
│ ▼ 人物模型   映射/浏览器/导出      │
│ ▼ 插件调用   mod注入开关/适配器×5  │
└────────────────────────────────────┘
   📋 规划：改原生设置式左右分栏（分类侧栏+设置行）
```

### 4.4 面板侧边对话栏（下轮·语义调整执行层）

```
┌───────────────────────────────┬─────────────┐
│                               │ AI 侧边栏   │
│   任意原生面板                │─────────────│
│   （影片/模型/变形/粒子…）    │ 上下文:     │
│                               │ · 影片 XX   │
│                               │ · 选中: 角色1│
│                               │─────────────│
│                               │ 对话消息…   │
│                               │ [输入][发送]│
└───────────────────────────────┴─────────────┘
   展开开关悬浮于面板边缘；上下文自动限定当前面板与选中对象；
   AI 输出「针对当前对象」的修改 → 确认后执行（走正规通道可撤销）
```

### 4.5 锚点跟随入口（三层）

```
约束面板（AI工具→IK调整）      角色属性页(2.7)         渲染链(2.7)
┌──────────────────┐        ┌──────────────┐      FormPoseEvents
│ 模式: 脚部贴地 ▾ │        │ AI 锚点跟随  │      MODEL_POSE
│ 强度: 0.8        │        │ （同款控件） │      动画/IK 之后补偿
│ 释放角度: 25°    │        └──────────────┘      （贴地 Y 回位）
└──────────────────┘
        共享 AIAnchorConfig.COMPONENT（状态一致）
```

---

## 五、核心数据流

```
【生成流】对话/模板 → AI 服务 → storyboard/motion JSON
        → 解析为按钮 → 执行 → Films.mergeInto / PreviewSystem.stage
        → 时间轴同步 → AIUndoManager 登记 → Ctrl+Z 可回退

【动捕流】视频文件 → FFmpeg 抽帧 → ONNX 姿态 → 骨骼映射(六骨)
        → 平滑/脚锁 → MotionData → 导入目录/预览 → 烘焙

【上下文流】面板切换/选中变化 → AIWorkspaceContext.refresh
        → AI 编辑器提示词 / 侧边栏 / 助手菜单 读取

【mod 流】Loader 元数据+注册表+知识库+适配器 → ModInfo[]
        → AIContextService.digest → 分镜/对话提示词
        → mods_report.json（外部共享）
```

---

## 六、路线图（基于 2026-09-25 决策）

### P0 · 界面打磨
- [ ] AI 设置界面改原生设置式左右分栏（分类侧栏+设置行）
- [ ] 全部弹窗/面板滚动与显示问题清零（IK 面板已修，其余逐个排查）
- [ ] AI 编辑器细节：时间轴缩放、消息区性能（长对话虚拟化）

### P1 · 语义调整执行层（侧边对话栏）
- [ ] 面板边缘悬浮展开开关 + 侧边栏容器（任意面板）
- [ ] 上下文限定：AIWorkspaceContext 面板级快照注入提示词
- [ ] 执行层：AI 输出 → 结构化动作（改参数/生成分镜/应用动作）→ 确认 → 正规通道执行 → 登记撤销
- [ ] 优先支持面板：影片编辑器 → 模型编辑 → 变形 → 其余

### P1 · 锚定深化
- [ ] 绑定真实模型骨骼（替换演示链）：从选中角色的 Form 骨骼构建链
- [ ] 2.5/2.6 树可用性（新事件缺席时的降级入口）
- [ ] 1.21.11 渲染链可视化（GPU 管线下重做锚点标记）

### P2 · 视频识别打磨（核心功能）
- [ ] 进世界组件检查引导优化、识别结果预览编辑
- [ ] 与 AI 生成联动（识别动作 + AI 修正/混合）

### P2 · 版本跟进与文档
- [ ] 上游 2.7 新提交跟进重基；1.20.4 上游合入新事件后恢复缺席功能
- [ ] zh_tw 繁体校对；wiki 持续补充

---

## 七、历史遗留问题处置状态

| 遗留问题 | 状态 |
|---|---|
| AI 功能割裂（生成/辅助混杂） | ✅ 两线分离（编辑器 vs 原生面板+AI 按钮） |
| 界面无滑条/显示不全 | 🔶 IK 面板已修；其余在 P0 清零 |
| AI 设置不像原生 | 🔶 现为折叠卡片可用；P0 改左右分栏 |
| 主题切换无区别 | ✅ 全局色板 + 语义色（2026-09-25 修复） |
| 撤销缺失 | ✅ AIUndoManager 全覆盖 |
| 汉化不全 | ✅ 44 键替换 + 语义键命名；zh_tw 待校对 |
| 上游版本漂移 | ✅ BBSFSCompat + 分支矩阵 |
